<!DOCTYPE html>
<html lang="pl">
	<head>
		<meta charset="utf-8" />

		<title>{{ title }}</title>
	</head>
	<body>
		<section>
		Itemki:
		{{ items_1 }}
		</section>

		<section>
		Treść:
		{{ content }}
		</section>
	</body>
</html>